# Chromebrew settings

# Do not edit this file. It will be overwritten by updates.

# Switch TMPDIR to chromebrew's TMPDIR
export TMPDIR="${CREW_PREFIX}/tmp"

# Give TMPDIR standard permissions
sudo chmod 1777 "${TMPDIR}"
